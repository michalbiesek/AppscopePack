output: default
streamtags: []
groups: {}
asyncFuncTimeout: 1000
functions:
  - id: drop
    filter: source!='http.req' && source!='http.resp'
    disabled: null
    conf: {}
    description: Drop everything besides HTTP response/request
  - id: eval
    filter: "true"
    disabled: false
    conf:
      keep:
        - _time
        - data*
      remove:
        - "*"
      add: []
    description: Save _time and data
  - id: eval
    filter: "true"
    disabled: false
    conf:
      add:
        - value: Object.assign(__e, data)
          name: ""
      remove:
        - data*
    description: Promote data structure
