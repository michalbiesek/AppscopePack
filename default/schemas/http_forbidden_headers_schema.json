{
   "type": "object",
   "allOf": [
      { "not": { "required": ["X-HTTP-Method"] } },
      { "not": { "required": ["X-HTTP-Method-Override"] } },
      { "not": { "required": ["X-Method-Override"] } },
      { "not": { "required": ["X-Original-URL"] } }
   ]
}